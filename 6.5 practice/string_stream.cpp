#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    string s, word, new_s;
    getline(cin, s);

    stringstream s_stream(s);

    if (s_stream >> word) cout<<word<<endl;
    if (s_stream >> word) cout<<word<<endl;
    if (s_stream >> word) cout<<word<<endl;
    

    return 0;
}